{
  "name": "failing-cli",
  "version": "1.0.0",
  "description": "A failing npm CLI for demo: bin entry points to missing file, no governance files",
  "bin": { "failing-cli": "./bin/failing-cli.js" },
  "engines": { "node": ">=18" },
  "license": "MIT"
}
